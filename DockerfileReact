FROM node:16.17.0 as build
WORKDIR /app
COPY nlp-react/package.json .
RUN npm install
COPY nlp-react/ .
RUN npm run build

FROM nginx
COPY ./nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/build /usr/share/nginx/html