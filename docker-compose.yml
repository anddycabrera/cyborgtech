version: "3.8"

services:
  web:
    build:
      context: .
      args:
        - ENVIRONMENT=${ENVIRONMENT}
    volumes:
      - .:/app
    command: /app/startup.sh
    container_name: starlogs
    ports:
      - "${PORT}:8000"
    restart: always